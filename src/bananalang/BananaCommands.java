package src.bananalang;

/**
 * This enum represents the commands that can be executed by the Banana language.
 */
public enum BananaCommands {
    PUSH_ONE,
    ADD,
    MULTIPLY,
    PRINT,
    CLEAR
}
